<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wedding.mapper.MenusMapper">
    <select id="permissionList" parameterType="com.wedding.domain.entity.Users" resultType="java.lang.String">
        SELECT
           distinct m.PERMS
        FROM menus m
                 INNER JOIN role_menus rm ON m.menu_id = rm.menu_id
                 INNER JOIN user_roles ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{id}
    </select>
</mapper>
